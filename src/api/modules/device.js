import instance from '@/api/request';
const baseUrl = '/api'

export default{
  // 获取设备列表
  // getModelList: (params) => {
  //   return instance.get(`${baseUrl}/poros-permission/secOrg/bizList`, {params});
  // },
}
